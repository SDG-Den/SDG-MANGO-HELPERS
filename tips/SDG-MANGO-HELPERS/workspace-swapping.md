# Workspace Swapping

The marker/swapmarked system lets you swap all windows between two workspaces.

1. Focus the workspace you want to mark, then run the marker bind.
2. Move to the target workspace and run the swap bind.
3. All windows on both workspaces are exchanged.

State is stored in `~/.config/marker.state` and `~/.config/marker.workspace`.
