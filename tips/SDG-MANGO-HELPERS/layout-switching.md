# Layout Switching

Use `manlayout` to quickly switch between window layouts using fzf.

Available layouts include scroller, tile, grid, deck, fair, monocle, and dwindle —
each in horizontal and vertical variants.
