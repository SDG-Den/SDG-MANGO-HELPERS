# Config Editor

Use `manconf` to browse and edit your mango config files with fzf and bat previews.
Use `manmenu` for an interactive config key explainer with live documentation browser.

The config viewer reads from `~/.local/SDG-MANGO-CONF/options.list` to explain each key
and open relevant documentation in Firefox.
