# Bar Presets

Use `dmsbars` to toggle individual bars (top, taskbar, status, left/right sidebar,
dock) or apply full presets like classic, windows-like, mac-like, combo, and sidebar.

Presets are applied via `dms ipc call` commands.
