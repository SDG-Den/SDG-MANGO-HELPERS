# manmenu — Interactive Config Browser (Replaced)

**manmenu has been replaced by `mangoconf view`.**

Run `mangoconf view` to browse config files with line-by-line explanations and documentation links. See [203-mangoconf.md](203-mangoconf.md) for details.

The underlying scripts (`checker.sh`, `browser.sh`, `options.list`) remain the same.
